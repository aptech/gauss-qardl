# Synthetic Interval Outputs

This location stores deterministic interval and confidence-band fixtures.

Current stored fixtures cover seeded QARDL block-bootstrap intervals for beta,
gamma, and phi, plus seeded QIRF point estimates, confidence bands, and
bootstrap diagnostics.

TODO: add stored forecast interval baselines after interval policy is finalized
for ARDL-family forecasts and published QARDL bootstrap targets.
