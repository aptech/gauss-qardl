# Published Validation Cases

Add GAUSS validation cases here after exact published datasets,
transformations, and expected outputs are available.

The validation runner keeps published-result cases separate from synthetic
benchmarks. Use:

```powershell
powershell -ExecutionPolicy Bypass -File tests/run_validation_benchmarks.ps1 -IncludePublished
```

when published cases are added.

